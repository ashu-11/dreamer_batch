// we're creating the context for this applcation 
// setup the context for the application 

import { createContext} from "react";

const counterConext = createContext();
export default counterConext;


//  prodcut---------->audince{consumer}--------how they will distibute/awareness about their---provider
